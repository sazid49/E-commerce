<div class="form-group">
    <label for="">{{ $title }} :<i class="text-danger text-bold">*</i></label>
    <input type="{{ $type }}" name="{{ $name }}" class="form-control" value="{{ $value }}"
        placeholder="{{ $placeholder }}">
</div>
